# partitioning

Enumerate ways to fill `box` slots with positive integers that sum to `target`, in non-decreasing order.

Practice coding.
```
python3 main.py
```

Enter the box size, then the target sum.
